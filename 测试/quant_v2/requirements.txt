# ============================================================
# Quant V2 requirements.txt
# 由项目实际 import 分析生成（2026-09-24，Python 3.13.15）
#
# 三段式：核心 / 测试 / 可选数据源
# 环境：Python 3.13（pandas 3.x，pure-3.13 兼容）
# ============================================================

# ---------- 核心运行依赖 ----------
# 绩效计算 / 净值分析 / 报告数据框
pandas>=2.2,<3.1

# ---------- 测试依赖 ----------
# 全量测试：python -m pytest tests/
pytest>=8.0

# ---------- 数据源：akshare（免费，无需 apikey） ----------
# 需在 Python 3.13 下运行，须先加载 utils/compat.py 兼容补丁
akshare>=1.15

# ---------- 数据源：掘金量化 MyQuant（可选，需 apikey） ----------
# 启用掘金时取消下方注释并安装（掘金官方 SDK 依赖较重，默认不装）
# gm>=3.5.98

# ---------- 数据源：matplotlib（报告可视化，可选） ----------
# matplotlib>=3.8
